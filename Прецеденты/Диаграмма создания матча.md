```plantuml
@startuml

actor Организатор

activate Stadium

Организатор -> Stadium : Autharization(user_id)
Организатор -> Stadium : OpenEditingMatch()
Организатор -> Stadium : CreateMatch(datetime_start, team_first, team_second, sectors_rows_seats_id[])

Stadium -> Match : create(datetime_start, team_first, team_second)
activate Match


loop для всех секторов матча

Stadium -> Match : CreateChild(sector_id, rows_seats_id[])
Match -> Sector : create(sector_id)
activate Sector


loop для всех рядов сектора

Match -> Sector : CreateChild(row_id, seats_id[])
Sector -> Row : create(row_id)
activate Row


loop для всех мест в ряду

Sector -> Row : CreateChild(seat_id)
Row -> Seat : create(seat_id)
activate Seat
Row <- Seat : seat_object

end

Sector <- Row : row_object

end

Match <- Sector : sector_object

end

Stadium <- Match : match_object


Stadium -> Stadium : GenerateMatchReport(datetime_start, team_first, team_second, sectors_rows_seats_id[])
Организатор <- Stadium : отчёт по параметрам созданного матча (дата проведения, команды, сектора, ряды, места)

Организатор -> Stadium : CloseEditingMatch()
Организатор <- Stadium : сообщение об успешном окончании операции

@enduml
```