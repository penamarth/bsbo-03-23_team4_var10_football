```plantuml
@startuml
class Main
{
    + main()
}
class Stadium
{
    - matches[]
    - GenerateMatchReport(datetime_start, team_first, team_second, sectors_rows_seats_id[])
    + Autharization(user_id)
    + CreateSale(type)
    + CloseSale()
    + MakePrice()
    + MakePayment(cash)
    + CreateTicket(match_datetime_start, grandstand_sector, grandstand_row, grandstand_seat)
    + CreateTicketSession(datetime_start, datetime_end)
    + CreateMatch(datetime_start, team_first, team_second, sectors_rows_seats_id[])
    + SelectMatchSeats(matches_id)
    + OpenEditingMatch()
    + CloseEditingMatch()
    + Quit()
}


abstract class Grandstand implements GrandstandParams
{
    + CreateChild(id)
    + ChangeChild(id)
    + GetChild(id)
    + GetBooking(seat_path[])
    + ChangeBooking(seat_path[])
}

entity Match extends Grandstand
{
    - datetime_start
    - datetime_end
    - team_first
    - team_second
    - sectors[]
    - tickets[]
    + ChangeDatetime(datetime_start, datetime_end)
    + ChangeTeams(team_first, team_second)
    + SaveTicket(ticket_object)
}

entity Sector extends Grandstand
{
    - sector_id
    - ticket_session_only
    - rows[]
}

entity Row extends Grandstand
{
    - row_id
    - seats[]
}

entity Seat implements GrandstandParams
{
    - seat_id
    - booking
}

entity Ticket implements TicketParams
{
    - match_datetime_start
    - grandstand_sector
    - grandstand_row
    - grandstand_seat
    # price
    + ChangeSeat(grandstand_sector, grandstand_row, grandstand_seat)
}

entity TicketSession extends Ticket implements TicketParams
{
    - ticket_session_id
    - datetime_start
    - datetime_end
    + ChangeDatetime(datetime_start, datetime_end)
}

entity Sale
{
    - datetime
    + full_price
    - tickets[]
    - CalcPrice(price)
    + CreateTicket(match_datetime_start, grandstand_sector, grandstand_row, grandstand_seat)
    + CreateTicketSession(datetime_start, datetime_end)
    + MakePrice()
    + MakePayment(cash)
}

class Payment
{
    - MakePayment(cash)
}

class Log
{
    - sale_operations[]
    - MakeLogSale(datetime, full_price, tickets[])
}

interface GrandstandParams
{
    + GetID()
    + GetBooking()
    + ChangeBooking()
}

interface TicketParams
{
    + GetPrice()
    + ChangePrice(price)
}

Main "1" --> "1" Stadium : Обращается
Stadium "1" *-- "0..*" Match : Содержит
Stadium "1" --> "0..*" Sale : Создаёт
Stadium "1" --> "1" Log : Обращается

Match "1" *-- "1..*" Sector : Содержит
Sector "1" *-- "1..*" Row : Содержит
Row "1" *-- "1..*" Seat : Содержит

Match "1" *-- "1..*" Ticket : Содержит
Seat "1" <.. "1" Ticket : Ссылается

Sale "1" --> "1" Payment : Обращается
Sale "1" --> "1..*" Ticket : Создаёт
Sale "1" --> "0..*" TicketSession : Создаёт
Match "1" <-- "1" Sale : Обращается

Log "1" *-- "*" Sale : Содержит
@enduml
```